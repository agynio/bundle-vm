apiVersion: v1
kind: Namespace
metadata:
  name: platform
---
apiVersion: v1
kind: Namespace
metadata:
  name: minio
---
apiVersion: v1
kind: Namespace
metadata:
  name: openfga
# agyn-workloads is deliberately absent: the agyn-platform chart creates it, and
# a namespace this file made first has no Helm ownership metadata, so the
# install fails with "exists and cannot be imported into the current release".
